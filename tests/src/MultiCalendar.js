/* eslint-env jasmine */
import moment from 'moment';

describe('An instance of the MultiCalendar class should', () => {
  // ===================
  // Presentation
  // ===================
  xit('create a wrapper for calendars');

  // ===================
  // Functionality
  // ===================
  xit('create the appropriate number of calendars on initilisation');
  xit('load data from given URL on initialisation');
  xit('pass loaded data to Calendars correctly');

  xit('add a new calendar with the same number of days as other already present calendars');
  xit('find function should return the correctly found calendar if it exists');
  xit('find function should return undefined if calendar not found');
});
